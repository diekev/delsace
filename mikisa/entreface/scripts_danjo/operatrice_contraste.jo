disposition "operatrice_contraste" {
    ligne {
	    étiquette(valeur="Nom Calque")
		liste(valeur="image"; attache=nom_calque; infobulle="Nom du calque à modifier.")
	}
	ligne {
	    étiquette(valeur="Pivot")
		décimal(valeur=0.5; attache=pivot; min=0; max=1)
	}
	ligne {
	    étiquette(valeur="Contraste")
		décimal(valeur=1.0; attache=contraste)
	}
}
