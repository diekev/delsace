disposition "operatrice_3d_grille" {
    ligne {
	    étiquette(valeur="Lignes")
		entier(valeur=2; attache=lignes; min=2; animable)
	}
	ligne {
	    étiquette(valeur="Colonnes")
		entier(valeur=2; attache=colonnes; min=2; animable)
	}
	ligne {
	    étiquette(valeur="Taille X")
		décimal(valeur=1.0; attache=taille_x; min=0.0; animable)
	}
	ligne {
	    étiquette(valeur="Taille Y")
		décimal(valeur=1.0; attache=taille_y; min=0.0; animable)
	}
	ligne {
	    étiquette(valeur="Position")
		vecteur(valeur="0,0,0"; attache=position; animable)
	}
	ligne {
	    étiquette(valeur="Rotation")
		vecteur(valeur="0,0,0"; attache=rotation; animable)
	}
	ligne {
	    étiquette(valeur="Taille")
		vecteur(valeur="1,1,1"; attache=taille; animable)
	}
}
