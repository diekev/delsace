disposition "operatrice_point3d_traduction" {
    ligne {
	    étiquette(valeur="Vieux Min")
		décimal(valeur=0.0; attache=vieux_min; min=-1; max=1)
		}
	ligne {
	    étiquette(valeur="Vieux Max")
		décimal(valeur=1.0; attache=vieux_max; min=-1; max=1)
	}
	ligne {
	    étiquette(valeur="Neuf Min")
		décimal(valeur=0.0; attache=neuf_min; min=-1; max=1)
	}
	ligne {
	    étiquette(valeur="Neuf Max")
		décimal(valeur=1.0; attache=neuf_max; min=-1; max=1)
	}
}
