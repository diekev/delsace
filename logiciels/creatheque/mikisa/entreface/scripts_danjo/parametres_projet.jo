disposition "Paramètres Projet" {
    ligne {
        étiquette(valeur="Largeur")
	entier(valeur=1920; min=4; max=8096; attache=largeur)
    }
    ligne {
        étiquette(valeur="Hauteur")
	entier(valeur=720; min=4; max=8096; attache=hauteur)
    }
}
