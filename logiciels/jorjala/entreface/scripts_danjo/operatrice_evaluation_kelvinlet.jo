disposition "operatrice_evaluation_kelvinlet" {
    ligne {
	    étiquette(valeur="Temps")
		décimal(valeur=1.0; min=1; attache=temps; animable)
	}
	ligne {
		étiquette(valeur="Début")
		entier(valeur=1; attache=début; animable)
	}
	ligne {
		étiquette(valeur="Intégration")
		énum(valeur="rk4"; attache=intégration;
		     items=[{nom="1er-ordre", valeur="euler"},
			        {nom="Runge-Kutta 4", valeur="rk4"}])
	}
}
