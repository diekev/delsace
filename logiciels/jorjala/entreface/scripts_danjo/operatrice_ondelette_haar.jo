disposition "operatrice_ondelette_haar" {
    ligne {
	    étiquette(valeur="Nom Calque")
		liste(valeur="image"; attache=nom_calque; infobulle="Nom du calque à modifier.")
	}
	ligne {
	    étiquette(valeur="Itérations")
		entier(valeur=2; attache=itérations; min=1; max=10; animable)
	}
}
