/* Pas d'erreur de compilation si une expression d'appel possède un typage
 * d'un paramètre
 * Catégorie : [syntaxage, validation sémantique]
 */

// puisque fonc est malécrit, ceci est considéré comme un appel, mais les appels ne doivent pas pouvoir déclarer de type !
fonction :: fon (a: z32)
{
}

principale :: fonc ()
{
}
