/* Crash si un type polymorphique n'a pas ses paramètres dans une déclaration
 * de fonction.
 * Catégorie : [crash, type polymorphique, paramètre fonction]
 */

importe Fondation
importe Math

/* Degrés est polymorphique, mais la fonction n'est pas notée comme tel. */
imprime_degrés :: fonc (d: Degrés)
{
    imprime("%\n", d)
}

principale :: fonc ()
{
    d: Degrés(r32)
    imprime_degrés(d)
}
