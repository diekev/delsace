/* Crash si nous définissons un type pointeur polymorphique dans un type fonction.
 * Similaire à bug86.
 * Voir note « crash si le type polymorphique est déclaré dans le rappel » dans le module Exétron. */

fonction_polymorphique :: fonc (rappel: fonc(*$T)(rien), données_rappel: *T)
{
    rappel(données)
}

principale :: fonc () -> z32
{   
    retourne 0
}