/* Impossible d'avoir un type de fonction contenant un type monomorphé.
 * NOTE : voir déclarations dans Géométrie.
 * Catégorie : [validation sémantique, type fonction, type polymorphique]
 */

Polymorphe :: struct ($T: type_de_données) {
    x: T
}

TypeRappel :: fonc(Polymorphe(r32))(rien)

Interface :: struct {
    rappel: fonc(Polymorphe(r32))(rien)
}

principale :: fonc ()
{
}
