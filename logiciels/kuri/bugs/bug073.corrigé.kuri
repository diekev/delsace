/* Aucune erreur de compilation quand le nom d'une structure n'est pas une
 * chaine simple.
 * Catégorie : [syntaxage]
 */

ParamètresGénérationTige (object) :: struct {
    x: z32
}

principale :: fonc ()
{
}
