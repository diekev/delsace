/* Pas d'erreur de compilation si nous tentons de décaler un bit de plus de place que le type le permet. */

principale :: fonc () -> z32
{
    /* Pour z32, 31 est le maximum. */
    _ := 1 << 32
    retourne 0
}
