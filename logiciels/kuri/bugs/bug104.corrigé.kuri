/* Crash si la définition d'une fonction externe n'a pas de type de retour,
 * mais que la flèche est présente.
 * Catégorie : [crash, syntaxage]
 */

principale :: fonc () -> z32
{
    retourne 0
}

foo :: fonc () -> #externe libc