/* Aucune erreur de compilation quand un type variadique est utilisé dans une structure.
 * Catégorie : [validation sémantique, type structure, type variadique]
 */

Maçon :: struct {
    param: ...
}

principale :: fonc () -> z32
{
    retourne 0
}
