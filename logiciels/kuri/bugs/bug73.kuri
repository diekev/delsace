
/* Aucune erreur de compilation quand les paramètres des structures n'ont pas de types ou
 * d'informations de polymorphie. */
ParamètresGénérationTige (object) :: struct {
    x: z32
}

principale :: fonc () -> z32
{
    retourne 0
}