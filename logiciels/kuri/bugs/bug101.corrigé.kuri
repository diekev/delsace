/* Erreur de génération de code C si un type variadique est utilisé
 * dans une expression quelconque.
 * Catégorie : [génération code C, type variadique]
 */

Struct :: struct {
    x: z32
}

principale :: fonc ()
{
    x: ...Struct
    si type_de(x) != ...Struct {
        exit(1)
    }
}
