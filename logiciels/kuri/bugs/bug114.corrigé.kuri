/* Impossible de convertir implicitement un opaque de pointeur vers un *octet.
 * Catégorie : [validation sémantique, conversion implicite, type opaque]
 */

importe Chaine

principale :: fonc ()
{
    c := ChaineC(nul)
    _ : *octet = c
}
