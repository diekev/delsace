
/* Aucune erreur de compilation quand le nom d'une structure n'est pas une chaine simple. */
ParamètresGénérationTige (object) :: struct {
    x: z32
}

principale :: fonc () -> z32
{
    retourne 0
}