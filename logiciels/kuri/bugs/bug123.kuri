/* Crash si l'expression par défaut d'une rubrique d'une structure référence un
 * rubrique de celle-ci.
 * Catégorie : [crash, type structure, expression rubrique]
 */

MaStructure :: struct {
    x : z32 = 1
    y := x + 1
}

principale :: fonc ()
{
    m: MaStructure

    si m.x != 1 {
        exit(1)
    }
}
