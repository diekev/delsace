/* Impossible de comparer un opaque de pointeur avec nul.
 * Catégorie : [validation sémantique, opérateur, type opaque]
 */

importe Chaine

principale :: fonc ()
{
    c := ChaineC(nul)
    saufsi c == nul {
        exit(1)
    }
}
