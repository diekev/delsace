// ------------------------------
// Cas : Les opérateurs booléens logiques ne fonctionnent pas sur des nombres réel.

foo :: fonc () -> rien
{
	t0 := 5.0 && 2.0;
}

// ------------------------------
// Cas : Les opérateurs arithmétiques entiers ne sont pas définis pour les nombres réel.

foo :: fonc () -> rien
{
	t0 := 5.0 % 2.0;
}

// ------------------------------
// Cas : Les opérateurs arithmétiques binaires ne sont pas définis pour les nombres réel.

foo :: fonc () -> rien
{
	t0 := 5.0 & 2.0;
}

