principale :: fonc () -> z32
{
    // Déclaration d'un tableau de 12 nombre entier relatif 32-bit
    tabl: [12]z32
    début := 1

    // Remplis le tableau avec des puissances de 2, de 1 à 4096
    // La boucle ne va jamais plus loin que les limites du tableau
    pour & val dans tabl {
        val = début
        début *= 2
    }

    retourne 0
}
