importe Fondation

principale :: fonc () -> z32
{
    imprime("Bonjour, tout le monde !\n")
    imprime("Nous allons imprimer les arguments :\n")

    args := arguments_ligne_commande()

    pour arg, index dans args {
        imprime("Argument % = %\n", index, arg)
    }

    retourne 0
}
