/* Enveloppes autours des algorithmes pour les tableaux et autres fonctions. */

tableau_inverse :: fonc (tableau: [..]$T) #enligne
{
    inverse(tableau.pointeur, tableau.pointeur + tableau.taille)
}
