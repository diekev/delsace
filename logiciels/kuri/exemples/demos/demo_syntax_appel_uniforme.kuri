importe "flux"

ajoute :: fonc(a : z32, b : z32) -> z32
{
    retourne a + b
}

principale :: fonc(args : []*z8) -> z32
{
    x := 1
    y := 2

    z := ajoute(x, y).ajoute(x)

    imprime_sans_format(z, '\n')

    retourne 0
}
