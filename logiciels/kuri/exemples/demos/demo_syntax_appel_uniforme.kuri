importe Fondation

ajoute :: fonc (a: z32, b: z32) -> z32
{
    retourne a + b
}

principale :: fonc () -> z32
{
    x := 1
    y := 2

    z := ajoute(x, y).ajoute(x)

    imprime("%\n", z)

    retourne 0
}
