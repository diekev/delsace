importe Fondation

N := 5

XYZ :: struct {
    a := 128.0
    b : type_de(a)
}

copie_vNvN :: fonc(a : [3]z32, b : [3]z32) -> rien
{
    pour v, i dans b {
        a[i] = v
    }
}

principale :: fonc () -> type_de(N)
{
    x := 5.0
    y : type_de(x)

    infos := info_de(type_de(y))

    imprime("%\n", mémoire(infos))

    imprime("N = %\n", N)

    z := faux

    w : type_de(z) = vrai

    a := 0

    b := a comme type_de(w)

    retourne 0
}
