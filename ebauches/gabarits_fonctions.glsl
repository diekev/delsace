void cos(in TYPE a, out TYPE r)
{
	r = cos(a);
}

void sin(in TYPE a, out TYPE r)
{
	r = sin(a);
}

void tan(in TYPE a, out TYPE r)
{
	r = tan(a);
}

void ajoute(in TYPE a, in TYPE b, out TYPE r)
{
	r = a + b;
}

void soustrait(in TYPE a, in TYPE b, out TYPE r)
{
	r = a - b;
}

void multiplie(in TYPE a, in TYPE b, out TYPE r)
{
	r = a * b;
}

void divise(in TYPE a, in TYPE b, out TYPE r)
{
	r = a / b;
}

void enligne(in TYPE a, in TYPE b, in TYPE c, out TYPE r)
{
	r = mix(a, b, c);
}

void hermite1(in TYPE a, in TYPE b, in TYPE c, out TYPE r)
{

}
